RSpec.describe Async::Background::Refresh do
  it "has a version number" do
    expect(Async::Background::Refresh::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
